<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agro e Sustentabilidade</title>
    <style>
        /* Paleta vinho */
        :root {
            --cor-primaria: #800020;
            --cor-secundaria: #A52A2A;
            --cor-texto: #ffffff;
            --cor-fundo: #f4f4f4;
            --cor-contraste: #333333;
        }

        body {
            background-color: var(--cor-fundo);
            color: var(--cor-primaria);
            font-family: Arial, sans-serif;
            transition: background-color 0.3s, color 0.3s;
            line-height: 1.6;
        }

        header {
            background-color: var(--cor-primaria);
            color: var(--cor-texto);
            padding: 1rem 0;
            text-align: center;
        }

        nav {
            text-align: center;
            margin: 1rem 0;
        }

        nav a {
            margin: 0 1rem;
            color: var(--cor-primaria);
            text-decoration: none;
            font-weight: bold;
        }

        section {
            padding: 2rem;
            max-width: 900px;
            margin: 0 auto;
        }

        .contraste {
            background-color: var(--cor-contraste);
            color: var(--cor-texto);
        }

        .buttons {
            margin: 1rem 0;
            text-align: center;
        }

        button {
            padding: 0.5rem 1rem;
            margin: 0 0.5rem;
            border: none;
            background-color: var(--cor-primaria);
            color: var(--cor-texto);
            cursor: pointer;
            font-size: 1rem;
        }

        button:hover {
            background-color: var(--cor-secundaria);
        }
    </style>
</head>
<body>
    <header>
        <h1>Agro e Sustentabilidade</h1>
        <p>Conectando a produção agrícola com a responsabilidade ambiental</p>
    </header>

    <nav>
        <a href="#sobre">Sobre</a>
        <a href="#sustentabilidade">S
